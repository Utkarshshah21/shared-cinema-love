
import { useWebRTC } from '@/hooks/useWebRTC';
import type { RemoteParticipant, UseWebRTCReturn } from './types';

// Re-export types
export type { RemoteParticipant, UseWebRTCReturn };

// Re-export the hook
export { useWebRTC };
